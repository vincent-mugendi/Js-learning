// concatenating strings
var firstName = 'John';
var lastName = 'fiddle';

let fullName = firstName + ' ' + lastName;
console.log(fullName);

let greet = 'Hello' + ' ' + firstName;
console.log(greet)

// Finding the length of a string
console.log(firstName.length)


// Whats the 2nd character in firstName
console.log(firstName[1])

// Whats the last character in firstName
console.log(firstName[3]) //find the length and minus 1 from it

// 